<h3>Selected extra ingredients of cookie</h3>
<ul>
  <li>Base: {{cookie.base}} - {{pricyfy (find-price @root.allBases cookie.base)}} PLN</li>
  {{#each cookie.addons}}
    <li>
      {{#unless ../noDeleteButton}}
        <a class="button button-small"
           href="/configurator/delete-addon/{{this}}">Delete</a>
      {{/unless}}
      Addon: {{this}} - {{pricyfy (find-price @root.allAddons this)}} PLN
    </li>
  {{/each}}
</ul>
<p>
  <strong>Total of {{pricyfy sum}} PLN</strong>
</p>
